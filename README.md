# Tendering_AI_Agent
This project is a Streamlit-based AI system for tender document Q&amp;A using RAG. It extracts PDF text, splits it into chunks, creates embeddings with SentenceTransformers, stores them in FAISS, reranks results, and uses Ollama LLaMA 3 to generate context-based, citation-supported answers with hallucination control.
